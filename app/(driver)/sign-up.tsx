// driver/sign-up.tsx
import React from 'react';
import Auth from '../(auth)/sign-up'; // Adjust the path as necessary

const DriverSignUp = () => {
  return <Auth isDriver={true} />;
};

export default DriverSignUp;
